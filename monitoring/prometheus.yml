# Prometheus scrape config for the QuicForge metrics stack.
# Enable with: docker compose --profile monitoring up
global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: quicforge
    metrics_path: /metrics
    static_configs:
      - targets: ["node:8080"]
